// @ts-check

/**
 * @type {import('next-i18next').UserConfig}
 */
const i18nConfig = {
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'tr'],
    localeDetection: false,
  },
};

export default i18nConfig;
