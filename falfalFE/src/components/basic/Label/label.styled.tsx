import styled from 'styled-components';

const StyledLabel = styled.label`
  font-weight: 400;
  font-size: 16px;
  line-height: 18px;
  color: var(--white);
  margin-bottom: 4px;
`;

export { StyledLabel };
